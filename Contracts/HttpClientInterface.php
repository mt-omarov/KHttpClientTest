<?php

namespace Kaa\HttpClient\Contracts;

interface HttpClientInterface
{
    //public function request(string $method, string $url, ?Options $options=null): ResponseInterface;
    //public function stream(ResponseInterface|iterable $responses, float $timeout = null): ResponseStreamInterface;

    /**
     * Returns a new instance of the client with new default options.
     */
    // public function withOptions(Options $options): static;
    // here will be methods of HttpClientInterface
}
